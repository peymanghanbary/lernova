export enum LanguageNames {
    fa = "fa",
    en = "en",
}

export enum FLEX_DIRECTION {
    rtl = "row-reverse",
    ltr = "row",
}

export interface cardType{
    id: number,
    symbol: string,
    isFlipped: boolean,   
}